## Deploy Automático Vercel

Este projeto usa Vercel para hospedagem do frontend.

### Configuração automática via vercel.json
- Build: `cd frontend && npm install && npm run build`
- Output: `frontend/dist`
- Framework: Vite
